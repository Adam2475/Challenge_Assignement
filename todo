1) DB schema | OK

2) relations within entities | OK

3) implement CRUD

4) endpoint to give back every task with a specific tag

5) endpoint to give back every task of a specific project

6) dockerize application